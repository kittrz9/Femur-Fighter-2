#ifndef TYPES_H
#define TYPES_H

// Probably should have more types than just vec2f eventually but it's better than having it in the entity file

typedef struct {
	float x, y;
} vec2f;

#define WIDTH 1600
#define HEIGHT 900

#endif
